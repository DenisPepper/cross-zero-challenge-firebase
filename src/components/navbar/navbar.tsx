import css from './navbar.module.css';

export const Navbar = () => {
    return (
        <div className={css.navbar}>
            <h2 className={css.title}>Cross-zero chat</h2>
        </div>
    );
}
